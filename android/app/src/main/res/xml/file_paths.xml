<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Score card PNGs are written to the root of the internal cache directory
         via Capacitor Filesystem (Directory.Cache). External storage is not used. -->
    <cache-path name="scoreday_cache" path="." />
</paths>
